import type{Metadata}from"next";import{redirect}from"next/navigation";import{createClient}from"../../../lib/supabase/server";import{DiceHistory}from"./dice-history";export const metadata:Metadata={title:"Dice Chronicle — Mythweave"};
export default async function DiceHistoryPage({searchParams}:{searchParams:Promise<{table?:string}>}){const supabase=await createClient();const{data:{user}}=await supabase.auth.getUser();if(!user)redirect("/auth");const{table:tableId}=await searchParams;if(!tableId)redirect("/dice");const{data:table}=await supabase.from("dice_tables").select("id,code").eq("id",tableId).maybeSingle();if(!table)redirect("/dice");return <DiceHistory tableId={table.id} code={table.code}/>}
