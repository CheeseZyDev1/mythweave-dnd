"use client";import{useEffect,useState}from"react";import{createClient}from"../../lib/supabase/client";import type{WorldBoss,WorldBossContribution}from"../../lib/combat/world-boss";
export function WorldBossPanel({tableId,characterId,readOnly,initialBoss,initialContributions}:{tableId:string;characterId:string|null;readOnly:boolean;initialBoss:WorldBoss|null;initialContributions:WorldBossContribution[]}){const[boss,setBoss]=useState(initialBoss);const[contributions,setContributions]=useState(initialContributions);const[busy,setBusy]=useState(false);const[message,setMessage]=useState("");useEffect(()=>{if(!boss)return;const supabase=createClient();const channel=supabase.channel(`world-boss-${boss.id}-${tableId}`).on("postgres_changes",{event:"UPDATE",schema:"public",table:"world_bosses",filter:`id=eq.${boss.id}`},payload=>setBoss(payload.new as WorldBoss)).on("postgres_changes",{event:"INSERT",schema:"public",table:"world_boss_contributions",filter:`table_id=eq.${tableId}`},payload=>setContributions(current=>[payload.new as WorldBossContribution,...current].slice(0,12))).subscribe();return()=>{void supabase.removeChannel(channel)}},[boss?.id,tableId]);async function strike(){if(!boss||!characterId)return;setBusy(true);setMessage("");try{const response=await fetch("/api/world-boss",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({bossId:boss.id,tableId,characterId})});const result=await response.json();if(!response.ok)throw new Error(result.error==="strike_cooldown"?"พักฟื้นพลัง 8 วินาทีก่อนโจมตีอีกครั้ง":"โจมตี World Boss ไม่สำเร็จ");setBoss(result.boss);setContributions(current=>[result.contribution,...current.filter(item=>item.id!==result.contribution.id)].slice(0,12));setMessage(`${result.contribution.character_name} สร้าง ${result.contribution.damage} damage`);}catch(caught){setMessage(caught instanceof Error?caught.message:"โจมตีไม่สำเร็จ")}finally{setBusy(false)}}if(!boss)return null;const percent=Math.max(0,(boss.hp_current/boss.hp_max)*100);return <section className="world-boss-panel"><header><div><small>GLOBAL RAID · SHARED HP</small><h2>{boss.name_th}</h2><p>{boss.title_th} · PHASE {boss.phase}</p></div><b>{boss.status.toUpperCase()}</b></header><div className="world-boss-hp"><i style={{width:`${percent}%`}}/><span>{boss.hp_current.toLocaleString("th-TH")} / {boss.hp_max.toLocaleString("th-TH")} HP</span></div><button disabled={busy||readOnly||!characterId||boss.status!=="active"} onClick={strike}>{busy?"กำลังส่งพลัง…":readOnly?"SPECTATOR · VIEW ONLY":"ร่วมโจมตี World Boss"}</button>{message&&<p>{message}</p>}<div className="world-boss-feed">{contributions.map(item=><span key={item.id}><b>{item.character_name}</b><i>-{item.damage} HP</i></span>)}</div></section>}
