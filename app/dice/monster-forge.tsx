"use client";
import{useEffect,useState}from"react";import{createClient}from"../../lib/supabase/client";import type{GeneratedMonster}from"../../lib/monsters/types";
export function MonsterForge({tableId,isDm,initialMonsters}:{tableId:string;isDm:boolean;initialMonsters:GeneratedMonster[]}){const[monsters,setMonsters]=useState(initialMonsters);const[challenge,setChallenge]=useState("standard");const[biome,setBiome]=useState("forest");const[busy,setBusy]=useState(false);const[message,setMessage]=useState("");useEffect(()=>{const supabase=createClient();const channel=supabase.channel(`monsters-${tableId}`).on("postgres_changes",{event:"INSERT",schema:"public",table:"generated_monsters",filter:`table_id=eq.${tableId}`},payload=>{const incoming=payload.new as GeneratedMonster;setMonsters(current=>current.some(item=>item.id===incoming.id)?current:[incoming,...current].slice(0,12));}).subscribe();return()=>{void supabase.removeChannel(channel);};},[tableId]);
async function generate(){setBusy(true);setMessage("");try{const response=await fetch("/api/monsters/generate",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({tableId,challenge,biome})});const result=await response.json();if(!response.ok)throw new Error(result.error==="cooldown"?"พลังสร้างอสูรยังไม่เสถียร รอ 10 วินาที":"สร้างอสูรไม่สำเร็จ");const incoming=result.monster as GeneratedMonster;setMonsters(current=>current.some(item=>item.id===incoming.id)?current:[incoming,...current].slice(0,12));setMessage(`ปลุก ${incoming.name_th} ขึ้นมาแล้ว`);}catch(caught){setMessage(caught instanceof Error?caught.message:"สร้างอสูรไม่สำเร็จ");}finally{setBusy(false);}}
return <section className="monster-forge"><header><div><small>PROCEDURAL BESTIARY · REALTIME</small><h2>โรงเพาะอสูร</h2></div><span>{isDm?"DM CONTROL":"PARTY VIEW"}</span></header>{isDm&&<div className="monster-controls"><select value={challenge} onChange={event=>setChallenge(event.target.value)}><option value="minion">Minion</option><option value="standard">Standard</option><option value="elite">Elite</option><option value="boss">Boss</option></select><select value={biome} onChange={event=>setBiome(event.target.value)}><option value="forest">ป่า</option><option value="cave">ถ้ำ</option><option value="swamp">หนองน้ำ</option><option value="ruins">ซากโบราณ</option></select><button onClick={generate} disabled={busy}>{busy?"กำลังปลุก…":"สุ่มอสูร"}</button></div>}{message&&<p className="monster-message">{message}</p>}<div className="monster-list">{monsters.length?monsters.map(monster=><article className={monster.challenge_tier} key={monster.id}><div><small>{monster.challenge_tier} · {monster.biome}</small><h3>{monster.name_th}</h3><span>{monster.traits?.signature??"สัญชาตญาณดิบ"}</span></div><dl><div><dt>HP</dt><dd>{monster.hp_max}</dd></div><div><dt>AC</dt><dd>{monster.armor_class}</dd></div><div><dt>ATK</dt><dd>+{monster.attack_bonus}</dd></div><div><dt>DMG</dt><dd>{monster.damage_dice}</dd></div></dl></article>):<div className="monster-empty">DM ยังไม่ได้สร้างอสูรสำหรับการเผชิญหน้าครั้งนี้</div>}</div></section>;}
