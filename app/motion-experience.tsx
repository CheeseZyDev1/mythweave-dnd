"use client";
import{AnimatePresence,motion,useReducedMotion}from"framer-motion";import{usePathname}from"next/navigation";import{useEffect,useState}from"react";
export function MotionExperience({children}:{children:React.ReactNode}){const pathname=usePathname();const reduced=useReducedMotion();const[diceBurst,setDiceBurst]=useState(0);useEffect(()=>{const burst=()=>setDiceBurst(value=>value+1);window.addEventListener("mythweave:dice-sfx",burst);return()=>window.removeEventListener("mythweave:dice-sfx",burst)},[]);return <><AnimatePresence mode="wait" initial={false}><motion.div className="motion-page" key={pathname} initial={reduced?false:{opacity:0,y:10,filter:"blur(4px)"}} animate={{opacity:1,y:0,filter:"blur(0px)"}} exit={reduced?undefined:{opacity:0,y:-6,filter:"blur(3px)"}} transition={{duration:reduced?0:.28,ease:[.22,1,.36,1]}}>{children}</motion.div></AnimatePresence>{!reduced&&<motion.div aria-hidden className="motion-ambient" animate={{rotate:[0,4,-3,0],scale:[1,1.08,1]}} transition={{duration:14,repeat:Infinity,ease:"easeInOut"}}>✦</motion.div>}<AnimatePresence>{diceBurst>0&&<motion.div aria-hidden className="motion-dice-burst" key={diceBurst} initial={{opacity:0,scale:.35,rotate:-30,y:25}} animate={{opacity:1,scale:1,rotate:15,y:0}} exit={{opacity:0,scale:1.5,y:-35}} transition={{duration:.55,type:"spring",bounce:.48}} onAnimationComplete={()=>setTimeout(()=>setDiceBurst(0),180)}>◈</motion.div>}</AnimatePresence></>}
