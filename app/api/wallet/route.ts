import{NextResponse}from"next/server";import{createClient}from"../../../lib/supabase/server";
const UUID=/^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i;
export async function POST(request:Request){const supabase=await createClient();const{data:{user}}=await supabase.auth.getUser();if(!user)return NextResponse.json({error:"unauthorized"},{status:401});const body=await request.json().catch(()=>null);const characterId=String(body?.characterId??"");const amountCopper=Number(body?.amountCopper);const reason=String(body?.reason??"").trim().slice(0,120);if(!UUID.test(characterId)||!Number.isInteger(amountCopper)||amountCopper===0||Math.abs(amountCopper)>100000000||!reason)return NextResponse.json({error:"invalid_request"},{status:400});const{data,error}=await supabase.rpc("adjust_character_wallet",{target_character_id:characterId,amount_copper:amountCopper,transaction_reason:reason}).single();if(error||!data){const code=error?.message.includes("insufficient")?"insufficient_funds":error?.message.includes("wallet not found")?"not_found":"adjust_failed";return NextResponse.json({error:code},{status:code==="insufficient_funds"?409:code==="not_found"?404:500});}return NextResponse.json({transaction:data});}
