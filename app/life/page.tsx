import type{Metadata}from"next";import{notFound,redirect}from"next/navigation";import{createClient}from"../../lib/supabase/server";import{LifeClient}from"./life-client";export const metadata:Metadata={title:"Life & Legacy — Mythweave"};export default async function LifePage({searchParams}:{searchParams:Promise<{character?:string}>}){const supabase=await createClient();const{data:{user}}=await supabase.auth.getUser();if(!user)redirect("/auth");const{character:id}=await searchParams;if(!id)redirect("/lobby");const[{data:character},{data:life,error},{data:events}]=await Promise.all([supabase.from("characters").select("id,name,hp_current,hp_max").eq("id",id).maybeSingle(),supabase.rpc("character_life_status",{target_character_id:id}),supabase.from("character_life_events").select("id,event_type,life_mode,cause,hp_after,created_at").eq("character_id",id).order("created_at",{ascending:false}).limit(12)]);if(!character||error||!life)notFound();return <LifeClient character={character} initialLife={life} events={events??[]}/>;}
